import { ReactElement } from "react";

import { PlannedApplication } from "@/core/shell/editor/PlannedApplication";

export function ParticlesEditorApplication(): ReactElement {
  return <PlannedApplication description={"Author particle effects and groups with a live preview."} />;
}
