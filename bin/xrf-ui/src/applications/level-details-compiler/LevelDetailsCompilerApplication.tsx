import { ReactElement } from "react";

import { PlannedApplication } from "@/core/shell/editor/PlannedApplication";

export function LevelDetailsCompilerApplication(): ReactElement {
  return <PlannedApplication description={"Build and light grass and detail-object data."} />;
}
