import { ReactElement } from "react";

import { PlannedApplication } from "@/core/shell/editor/PlannedApplication";

export function LevelAiCompilerApplication(): ReactElement {
  return <PlannedApplication description={"Build AI maps, level graphs and cross tables."} />;
}
