//! Giving every identity the frontend compares against a name the compiler resolves.

mod enumeration;
mod enumeration_member;
mod enumeration_subject;
mod enumerations;

pub(crate) use enumerations::Enumerations;
