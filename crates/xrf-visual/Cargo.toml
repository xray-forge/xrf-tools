[package]
name = "xrf-visual"
version = "0.1.0"
publish = false
edition = "2024"
rust-version.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
log = { workspace = true }
serde = { workspace = true }
specta = { workspace = true, optional = true }
xrf-db = { workspace = true }
xrf-error = { workspace = true }
xrf-vfs = { workspace = true }

[features]
# Emits TypeScript mirrors of the serialized types. Nothing in a normal build turns this on.
typescript-bindings = ["specta", "xrf-db/typescript-bindings", "xrf-vfs/typescript-bindings"]

[lints]
workspace = true

[dev-dependencies]
xrf-test-utils = { workspace = true }
