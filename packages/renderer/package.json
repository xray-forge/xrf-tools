{
  "name": "@xrf/renderer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "The deferred renderer every xrf-tools viewer draws through.",
  "exports": {
    ".": "./src/index.ts",
    "./fixtures": "./src/fixtures.ts",
    "./worker": "./src/worker.ts"
  },
  "imports": {
    "#/*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "eslint . --max-warnings 0 --cache --cache-location target/eslint/cache.json --cache-strategy content",
    "format": "prettier --write --ignore-path ../../.prettierignore \"**/*.(js|cjs|mjs|ts|md)\" && pnpm run lint --fix"
  },
  "dependencies": {
    "three": "catalog:",
    "@xrf/types": "workspace:*"
  },
  "devDependencies": {
    "@jest/globals": "catalog:",
    "@types/three": "catalog:",
    "@typescript/native": "catalog:",
    "eslint": "catalog:",
    "jest": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "@xrf/toolchain": "workspace:*"
  }
}
